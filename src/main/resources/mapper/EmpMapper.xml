<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xumob.pagehelperdemo1.mapper.EmpMapper">
    <select id="getPartEmp" resultType="Emp">
        SELECT id, name, salary, gender, birthday, dept_id FROM emp
    </select>
</mapper>